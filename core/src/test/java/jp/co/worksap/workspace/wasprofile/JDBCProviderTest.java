package jp.co.worksap.workspace.wasprofile;

import static org.hamcrest.CoreMatchers.is;
import static org.junit.Assert.assertThat;

import java.io.File;
import java.io.IOException;

import org.junit.Test;

public class JDBCProviderTest{
   /* @Test
    public void configureJDBCProvider() throws IOException {        
        int val=-1;
        JDBCProvider obj = new JDBCProvider();
        File f = new File("src\\test\\resources\\jdbc_config.json");
        val = obj.configure(f);       
        assertThat(val, is(0));
    }*/
}
